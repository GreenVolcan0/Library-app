<Window x:Class="LibraryApp.Views.AuthorsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="👤 Управление авторами"
        Width="750" Height="540"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="56"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/> <!-- Edit Panel -->
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}">
            <TextBlock Text="👤 Управление авторами" FontSize="16" FontWeight="Bold"
                       Foreground="White" VerticalAlignment="Center" Margin="20,0"/>
        </Border>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="12,10">
            <Button Content="➕ Добавить" Style="{StaticResource PrimaryButton}"
                    Margin="0,0,8,0" Click="BtnAdd_Click"/>
            <Button Content="✏️ Редактировать" Style="{StaticResource SecondaryButton}"
                    Margin="0,0,8,0" Click="BtnEdit_Click"/>
            <Button Content="🗑️ Удалить" Style="{StaticResource DangerButton}"
                    Click="BtnDelete_Click"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid x:Name="AuthorsGrid" Grid.Row="2" Margin="12,0,12,8"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single"
                  RowBackground="White" AlternatingRowBackground="#F8F9FA"
                  BorderBrush="#E0E0E0" BorderThickness="1"
                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EEEEEE"
                  ColumnHeaderHeight="38" RowHeight="36" FontSize="13"
                  MouseDoubleClick="AuthorsGrid_MouseDoubleClick">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#F5F5F5"/>
                    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="12,0"/>
                    <Setter Property="BorderBrush" Value="#E0E0E0"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="55"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="*"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="*"/>
                <DataGridTextColumn Header="Страна" Binding="{Binding Country}" Width="130"/>
                <DataGridTextColumn Header="Дата рождения" Binding="{Binding BirthDate, StringFormat=dd.MM.yyyy}" Width="140"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Close -->
        <Border Grid.Row="4" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <Button Content="Закрыть" Style="{StaticResource SecondaryButton}"
                    HorizontalAlignment="Right" Margin="20,0" VerticalAlignment="Center"
                    Click="BtnClose_Click"/>
        </Border>
    </Grid>
</Window>
